# MEXC Price Spread Monitor 🤖

Бот для мониторинга разницы между последней ценой и справедливой ценой на фьючерсах биржи MEXC. Отправляет уведомления в Telegram при обнаружении спреда 10%+.

## 📋 Возможности

- ✅ Мониторинг всех фьючерсных пар MEXC
- ✅ Сравнение последней цены (Last Price) и справедливой цены (Fair Price)
- ✅ Уведомления в Telegram при спреде 10%+
- ✅ Защита от спама (cooldown 30 минут для одной пары)
- ✅ Автоматическая обработка ошибок и повторные попытки

## 🚀 Установка

### 1. Установите Python 3.8+

Убедитесь, что у вас установлен Python 3.8 или выше.

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте Telegram бот

Файл `.env` уже создан с вашими данными:

- **Bot Token**: `8580047805:AAGBWn-PzmGSbRddnZ1D6evoQnwVevdmBFk`
- **Chat ID**: `8460086736`

## ▶️ Запуск

Запустите бота командой:

```bash
python main.py
```

Бот начнёт мониторинг всех фьючерсных пар MEXC и будет отправлять уведомления при обнаружении спреда.

## ⚙️ Настройки

Все настройки находятся в файле `config.py`:

- `MIN_SPREAD_PERCENT` - минимальный процент разницы для уведомления (по умолчанию 10%)
- `SCAN_INTERVAL` - интервал сканирования в секундах (по умолчанию 30)
- `ALERT_COOLDOWN` - пауза между повторными алертами для одной пары (по умолчанию 1800 сек / 30 минут)

## 📊 Формат уведомлений

Бот отправляет сообщения в таком формате:

```
🔴 СПРЕД ОБНАРУЖЕН!

📊 Пара: BTC_USDT

💰 Последняя цена: 45000.123456
⚖️ Справедливая цена: 40000.000000

📈 Разница: 12.50% (выше справедливой)

⏰ Проверить сейчас на MEXC!
```

## 🛑 Остановка

Нажмите `Ctrl+C` для остановки бота.

## 📁 Структура проекта

- `main.py` - главный модуль и точка входа
- `mexc_client.py` - клиент для работы с MEXC API
- `spread_analyzer.py` - анализ спреда цен
- `telegram_notifier.py` - отправка уведомлений в Telegram
- `config.py` - конфигурация бота
- `.env` - переменные окружения (токены, ID)

## ⚠️ Важно

- Не публикуйте файл `.env` в открытый доступ (он включён в `.gitignore`)
- При первом запуске бот загрузит все фьючерсные пары MEXC
- Рекомендуется запускать бота на VPS для непрерывного мониторинга
